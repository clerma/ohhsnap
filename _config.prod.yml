assets:
  cdn:
    # CDN disabled: ohhsnap.b-cdn.net was returning 301s back to apex on every
    # asset request, adding extra round-trips with no caching benefit. Apex is
    # already on Cloudflare's edge (cf-cache-status: HIT). Re-enable only after
    # Bunny is reconfigured to actually pull from origin.
    url: https://ohhsnap.b-cdn.net
    absolute_links: true
# _config.prod.yml
minify_build:
  enabled: true
  html: true
  css:  true
  js:   false
  json: true
  xml:  true
  svg:  true
purge_css_light:
  enabled: true
  html_globs: ["**/*.html"]
  css_globs:
    - "assets/css/theme.bundle.css"
    - "assets/css/theme-sans-serif.bundle.css"
    - "assets/css/libs.bundle.css"
    - "assets/css/custom.css"
  safelist:
    - "^fa-"
    - "^jarallax"
    - "^flickity-"
    - "^swiper-"
    - "^collapse$"
    - "^show$"
    - "^modal"
    - "^dropdown"
  log: true
